# Week 10 学习讲义：Django View / Middleware / 统一响应

本周目标：写可维护的 Django 接口，并理解中间件。

## 学习资料

- Views：<https://docs.djangoproject.com/en/5.2/topics/http/views/>
- URL dispatcher：<https://docs.djangoproject.com/en/5.2/topics/http/urls/>
- Middleware：<https://docs.djangoproject.com/en/5.2/topics/http/middleware/>
- JsonResponse：<https://docs.djangoproject.com/en/5.2/ref/request-response/#jsonresponse-objects>

## 核心内容

统一响应：

```python
def success(data=None):
    return JsonResponse({"code": 0, "message": "ok", "data": data})

def fail(message, code=1):
    return JsonResponse({"code": code, "message": message, "data": None})
```

中间件记录耗时：

```python
import time

class RequestTimeMiddleware:
    def __init__(self, get_response):
        self.get_response = get_response

    def __call__(self, request):
        start = time.time()
        response = self.get_response(request)
        response["X-Request-Time"] = str(round(time.time() - start, 4))
        return response
```

## 每日安排

- Day 1：复习 URL 和 View。
- Day 2：写列表接口。
- Day 3：写详情接口。
- Day 4：写新增接口。
- Day 5：封装统一响应。
- Day 6：写中间件。
- Day 7：整理请求链路。

## 练习题

1. `/articles/` 返回文章列表。
2. `/articles/<id>/` 返回详情。
3. `/articles/create/` 新增文章。
4. 所有接口统一响应。
5. 中间件返回耗时 Header。

## 验收标准

- 接口返回结构统一。
- 异常时返回友好错误。
- 能解释中间件执行顺序。

