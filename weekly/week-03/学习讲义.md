# Week 03 学习讲义：异常、日志、pytest

本周目标：让代码从“能跑”变成“出错能定位、逻辑能测试”。

## 学习资料

- 异常：<https://docs.python.org/3/tutorial/errors.html>
- logging：<https://docs.python.org/3/library/logging.html>
- pytest 入门：<https://docs.pytest.org/en/stable/getting-started.html>
- pytest fixture：<https://docs.pytest.org/en/stable/how-to/fixtures.html>

## 核心内容

异常处理不是把错误吞掉，而是让错误更可控：

```python
try:
    text = path.read_text(encoding="utf-8")
except FileNotFoundError:
    print(f"文件不存在: {path}")
```

日志比 `print` 更适合长期项目：

```python
import logging

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)
logger.info("start")
logger.exception("failed")
```

pytest 用于验证函数行为：

```python
def add(a, b):
    return a + b

def test_add():
    assert add(1, 2) == 3
```

## 每日安排

- Day 1：学习常见异常和 `try/except`。
- Day 2：给 Week 02 文件读写加异常处理。
- Day 3：学习 logging，替换 print。
- Day 4：学习 pytest 基础。
- Day 5：为正常路径写测试。
- Day 6：为异常路径写测试。
- Day 7：整理测试结果和复盘。

## 练习题

1. 文件不存在时输出友好提示。
2. JSON 格式错误时记录日志。
3. 保存 CSV 失败时抛出清晰异常。
4. 为 `clean_text` 写 3 个测试。
5. 为 `save_json/load_json` 写测试。
6. 使用 pytest 的 `tmp_path` 生成临时文件。

## 最终作业

改造 Week 02：

```text
src/week02/article_storage.py
tests/test_article_storage.py
```

## 验收标准

- 至少 6 个测试。
- `pytest` 能通过。
- 错误信息能定位问题。
- 日志里能看到开始、成功、失败。

