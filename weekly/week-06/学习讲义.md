# Week 06 学习讲义：HTML、XPath、BeautifulSoup

本周目标：从 HTML 中稳定提取字段。

## 学习资料

- BeautifulSoup：<https://www.crummy.com/software/BeautifulSoup/bs4/doc/>
- lxml XPath：<https://lxml.de/xpathxslt.html>
- CSS Selectors：<https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_selectors>

## 核心内容

BeautifulSoup：

```python
from bs4 import BeautifulSoup

soup = BeautifulSoup(html, "html.parser")
title = soup.select_one("h1").get_text(strip=True)
links = [a.get("href") for a in soup.select("a")]
```

XPath：

```python
from lxml import etree

tree = etree.HTML(html)
titles = tree.xpath("//h2/text()")
urls = tree.xpath("//a/@href")
```

字段提取要考虑缺失：

```python
node = soup.select_one(".title")
title = node.get_text(strip=True) if node else ""
```

## 每日安排

- Day 1：理解 HTML 树。
- Day 2：BeautifulSoup `find`、`select`。
- Day 3：CSS Selector。
- Day 4：XPath。
- Day 5：字段清洗。
- Day 6：解析容错。
- Day 7：完成 HTML 解析器。

## 练习题

1. 从本地 HTML 提取所有链接。
2. 提取标题、摘要、时间。
3. 字段缺失时返回空字符串。
4. 用 CSS Selector 和 XPath 各写一版。
5. 保存 JSONL。

## 最终作业

创建：

```text
src/week06/html_parser.py
```

## 验收标准

- 不用正则硬解析复杂 HTML。
- 解析规则集中管理。
- 输出前做字段清洗。
- 缺字段不崩溃。

