# Week 11 学习讲义：DRF / 公司魔改框架阅读

本周目标：掌握 DRF 基础，并学会阅读公司封装框架。

## 学习资料

- DRF Quickstart：<https://www.django-rest-framework.org/tutorial/quickstart/>
- Serializers：<https://www.django-rest-framework.org/api-guide/serializers/>
- APIView：<https://www.django-rest-framework.org/api-guide/views/>
- ViewSets：<https://www.django-rest-framework.org/api-guide/viewsets/>
- Routers：<https://www.django-rest-framework.org/api-guide/routers/>

## 核心内容

Serializer：

```python
from rest_framework import serializers

class ArticleSerializer(serializers.ModelSerializer):
    class Meta:
        model = Article
        fields = ["id", "title", "url", "published_at"]
```

ViewSet：

```python
from rest_framework.viewsets import ModelViewSet

class ArticleViewSet(ModelViewSet):
    queryset = Article.objects.all()
    serializer_class = ArticleSerializer
```

公司魔改框架阅读顺序：

```text
启动入口 -> settings -> urls -> middleware -> base view -> serializer -> model -> response -> exception
```

## 每日安排

- Day 1：DRF Quickstart。
- Day 2：Serializer。
- Day 3：APIView。
- Day 4：ViewSet / Router。
- Day 5：权限和分页。
- Day 6：阅读一个公司或开源项目。
- Day 7：写阅读报告。

## 练习题

1. 用 DRF 写文章列表接口。
2. 用 Serializer 校验字段。
3. 用 ViewSet 改写接口。
4. 加分页。
5. 找出一个项目的 BaseAPIView。

## 验收标准

- 能写 Serializer。
- 能写 ViewSet。
- 能解释公司封装和 Django/DRF 原生能力的区别。

