# Week 09 学习讲义：Django ORM

本周目标：用 Django Model 管理数据库。

## 学习资料

- Models：<https://docs.djangoproject.com/en/5.2/topics/db/models/>
- Making queries：<https://docs.djangoproject.com/en/5.2/topics/db/queries/>
- QuerySet：<https://docs.djangoproject.com/en/5.2/ref/models/querysets/>
- Migrations：<https://docs.djangoproject.com/en/5.2/topics/migrations/>

## 核心内容

Model：

```python
from django.db import models

class Article(models.Model):
    title = models.CharField(max_length=200)
    url = models.URLField(unique=True)
    published_at = models.DateTimeField(null=True, blank=True)
    content = models.TextField(blank=True)
```

迁移：

```bash
python manage.py makemigrations
python manage.py migrate
```

查询：

```python
Article.objects.create(title="t", url="https://example.com")
Article.objects.filter(title__icontains="python")
Article.objects.get(id=1)
```

## 每日安排

- Day 1：Model 字段。
- Day 2：迁移。
- Day 3：增删改查。
- Day 4：QuerySet。
- Day 5：唯一约束。
- Day 6：创建 Article、SpiderTask。
- Day 7：写 ORM 查询笔记。

## 练习题

1. 创建 Article。
2. 创建 SpiderTask。
3. 新增 5 条文章。
4. 查询标题包含 Python 的文章。
5. 更新任务状态。
6. 删除测试数据。

## 验收标准

- 迁移文件清晰。
- 能用 ORM 查询。
- 不手写拼接 SQL。
- 能解释 QuerySet 是惰性查询。

